# Syne Configuration
# Copy this file to .env

# Database connection (default works with docker-compose)
SYNE_DATABASE_URL=postgresql://syne:syne@localhost:5433/syne

# LLM Provider: google (default, free via OAuth) or openai
SYNE_PROVIDER=google
